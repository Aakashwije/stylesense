PORT=5000
LOG_LEVEL=info

# Redis (pub/sub from other services)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT (shared secret with core-api for verification)
JWT_SECRET=replace_me_with_a_long_random_string
